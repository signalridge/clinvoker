# 退出码

clinvk 的退出码含义。

## 总览

| 退出码 | 含义 |
|------|---------|
| 0 | 成功 |
| 1 | 错误（校验或执行失败） |
| 124 | 命令超时（若配置） |
|（后端）| 后端退出码（根命令/恢复会话） |

## 说明

- `clinvk [prompt]` 与 `clinvk resume` 会透传后端的非零退出码。
- `parallel` / `chain` / `compare` 任一失败返回 `1`。
