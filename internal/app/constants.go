package app

// Display constants for formatting output.
const (
	// maxSessionsDisplay is the maximum number of sessions to show in interactive picker.
	maxSessionsDisplay = 20

	// maxTitleDisplayLen is the maximum length for session titles in listings.
	maxTitleDisplayLen = 35

	// maxSessionTitleLen is the maximum length for session titles in session list.
	maxSessionTitleLen = 30

	// maxPromptDisplayLen is the maximum length for prompt display in session details.
	maxPromptDisplayLen = 80

	// maxTaskNameLen is the maximum length for task names in parallel output.
	maxTaskNameLen = 35

	// defaultMaxParallel is the default maximum parallel workers.
	defaultMaxParallel = 3

	// tableSeparatorWidth is the width of table separators in output.
	tableSeparatorWidth = 80
)
